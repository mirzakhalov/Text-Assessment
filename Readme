Run the following commands in your terminal first:
    - pip install nltk --user
    - pip install grammar_check --user
    - import nltk
    - nltk.download('punkt')
    - nltk.download('averaged_perceptron_tagger')